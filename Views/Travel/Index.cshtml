@model IEnumerable<Event>
@{
    ViewData["Title"] = "R. Freid Travel Planner";

    int id = Convert.ToInt32(ViewContext.RouteData.Values["id"]);
}
<h2>R. Freid Travel Planner</h2>
<p>Travel Planner for the Band Performances!</p>
<div class="text-center">
    <h1 class="display-4">Event Schedule</h1>
    
</div>
<table class="table">
    <thead>
        <tr><th>Event Name</th><th>Event City</th><th>Event Date</th></tr>
    </thead>
    <tbody>
        @foreach (Event e in ViewBag.Events)
        {
            <tr>
                <td>@e.EventName</td>
                <td>@e.EventCity</td>
                <td>@e.EventDate.</td>
                <td>
                    <a asp-action="Edit" asp-controller="Class" asp-route-id="@e.EventId"
                       class="btn btn-outline-dark">Edit</a>
                    <a asp-action="Delete" asp-controller="Class" asp-route-id="@e.EventId."
                       class="btn btn-outline-dark">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>